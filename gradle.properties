kotlin.code.style=official
# Reuse task outputs across builds and skip configuration on unchanged inputs — speeds up the
# repeated host `shadowJar` builds that deploy/build-podman.sh drives.
org.gradle.caching=true
org.gradle.configuration-cache=true
